#include <bits/stdc++.h>
#ifdef LOCAL_DEFINE
#include <dbg.h>
#else
#define dbg(...)
#endif

using namespace std;

int main() {
  //~ ifstream cin(".in");
  //~ ofstream cout(".out");
  ios::sync_with_stdio(0);
  cin.tie(0);

  

  cerr << "Time elapsed: " << 1.0 * clock() / CLOCKS_PER_SEC << " s.\n";
}
